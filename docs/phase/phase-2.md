# Iteration 2: CLI Arguments

**Goal:** Implement command-line argument parsing with clap so the application can accept all required inputs.

## Tasks

- [x] 2.1 Define `Args` struct with clap derive
- [x] 2.2 Add all 5 arguments (input, output, plugin, params, plugin-path)
- [x] 2.3 Print parsed arguments to verify

## Acceptance Criteria

**Test:** `cargo run -- --help` shows usage

## Dependencies

- Phase 1 complete

## Implementation Notes

Reference `docs/vision.md` for the Args struct definition and CLI design.
